<div class="container m-5">
  <div class="row">
    <div class="col-md-3"> 
      <table>
        <td>ジャンル検索</td>
        
        <% @genres.each do |genre|%>
        <tr>
      　<td><%=genre.name%></td>
        </tr>
        <%end%>
      </table>
    </div>　　
    　　　　　　　<!--<adminから情報きてる？>-->
    
    <div class="col-md-8">
      <p>商品一覧</p>
      <%= image_tag @product.get_image(200,100)%>
      <%=@product.name%>
      <%=@product.price%>
    </div>
  </div>
</div>